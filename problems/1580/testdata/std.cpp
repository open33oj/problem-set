#include <bits/stdc++.h>
using namespace std;
int main()
{
    char a;
    int cnt = 0;
    cin >> a; if (a == '3') cnt++; // 1
    cin >> a; if (a == '3') cnt++; // 2
    cin >> a; if (a == '3') cnt++; // 3
    cin >> a; if (a == '3') cnt++; // 4
    cin >> a; if (a == '3') cnt++; // 5
    cin >> a; if (a == '3') cnt++; // 6
    cin >> a; if (a == '3') cnt++; // 7
    cin >> a; if (a == '3') cnt++; // 8
    cout << cnt;
    return 0;
}