#include <bits/stdc++.h>
using namespace std;
double a, b;
int main()
{
    cin >> a >> b;
    cout << fixed << setprecision(8);
    cout << b / a / a << "\n";
    cout << 18.5 * a * a << " " << 24 * a * a << "\n";
    return 0;
}